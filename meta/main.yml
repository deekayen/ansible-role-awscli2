---
galaxy_info:
  namespace: deekayen
  role_name: awscli2
  author: David Norman
  description: Install AWS CLI v2 using the official installer instead of pip.

  github_branch: main
  issue_tracker_url: https://github.com/deekayen/ansible-role-awscli2/issues

  license: BSD

  min_ansible_version: "2.15"

  # Only listing what CI's molecule matrix actually tests (see
  # molecule/default/molecule.yml and .github/workflows/ci.yml).
  platforms:
    - name: Amazon
      versions:
        - all
    - name: Debian
      versions:
        - all

  galaxy_tags:
    - aws
    - awscli
    - awscli2

dependencies: []
