---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  yamllint . && ansible-lint && flake8
platforms:
  - name: instance
    image: "${MOLECULE_DISTRO}"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
